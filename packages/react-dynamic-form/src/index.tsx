import type { CreateForm, CreateFormReturnType } from "./types/index.js";

export function create({
  
  
}: CreateForm): CreateFormReturnType {
  return {
    DynamicForm: (<div>asd</div>) // todo separate generated component
  }
}